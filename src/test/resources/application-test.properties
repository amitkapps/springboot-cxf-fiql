#This config file contains overrides on top of application.properties when 'test' is the active spring profile

#the sql file that will be run before the tests
spring.datasource.schema = classpath:schema-h2.sql
# Don't validate the schema
spring.jpa.hibernate.ddl-auto = none